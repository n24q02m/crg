{
  "name": "better-code-review-graph",
  "description": "Knowledge graph for token-efficient code reviews -- semantic search and call-graph resolution across your codebase.",
  "version": "3.26.20",
  "userConfig": {
    "EMBEDDING_MODELS": {
      "type": "string",
      "title": "Embedding model selection (optional)",
      "description": "CSV 'provider/model,...'; selects the first entry without runtime fallback. Empty = local fastretrieval registry. Example: cohere/embed-v4.0 (paid; exact 1024 dimensions).",
      "required": false
    },
    "LOCAL_EMBEDDING_MODEL": {
      "type": "string",
      "title": "Local embedding model (optional)",
      "description": "Built-in fastretrieval model ID, or a local directory containing fastretrieval-manifest.json. External IDs require LOCAL_EMBEDDING_DIM.",
      "required": false
    },
    "LOCAL_RERANK_MODEL": {
      "type": "string",
      "title": "Local reranker model (optional)",
      "description": "Fastretrieval TextCrossEncoder model ID; empty = reranking disabled.",
      "required": false
    },
    "LOCAL_EMBEDDING_DIM": {
      "type": "number",
      "title": "Local embedding dimension",
      "description": "Required output dimension for an external LOCAL_EMBEDDING_MODEL ID without a manifest.",
      "required": false
    },
    "LOCAL_EMBEDDING_MODEL_FILE": {
      "type": "string",
      "title": "Local embedding model file",
      "description": "ONNX file path inside a manifest-backed local artifact directory.",
      "required": false
    },
    "LOCAL_EMBEDDING_POOLING": {
      "type": "string",
      "title": "Local embedding pooling",
      "description": "Explicit pooling for an external ID without a manifest: CLS, MEAN, LAST_TOKEN, or DISABLED.",
      "required": false
    },
    "LOCAL_EMBEDDING_NORMALIZE": {
      "type": "boolean",
      "title": "Normalize local embeddings",
      "description": "Apply explicit L2 normalization for an external ID without a manifest.",
      "required": false
    },
    "SUMMARY_MODELS": {
      "type": "string",
      "title": "Summarizer model chain (optional)",
      "description": "CSV 'provider/model,...' for graph summarize; selects the first entry without runtime fallback. Empty = summaries disabled. Example: openrouter/minimax/minimax-m3:free",
      "required": false
    },
    "EMBEDDING_API_BASE": {
      "type": "string",
      "title": "Embedding endpoint (optional)",
      "description": "Provider-compatible endpoint or CF AI Gateway embedding URL; SSRF-guarded.",
      "required": false
    },
    "LLM_API_BASE": {
      "type": "string",
      "title": "Completion endpoint (optional)",
      "description": "Provider-compatible base URL or CF AI Gateway OpenRouter route; SSRF-guarded.",
      "required": false
    },
    "OPENROUTER_API_KEY": {
      "type": "string",
      "title": "OpenRouter API key (optional)",
      "description": "Credential for explicitly selected openrouter/ completion models.",
      "sensitive": true,
      "required": false
    },
    "JINA_AI_API_KEY": {
      "type": "string",
      "title": "Jina AI API key (optional)",
      "description": "Credential for explicitly selected jina_ai/ embedding models. An empty EMBEDDING_MODELS selection uses local ONNX; keys alone do not select cloud. https://jina.ai/api-dashboard/",
      "sensitive": true,
      "required": false
    },
    "GEMINI_API_KEY": {
      "type": "string",
      "title": "Gemini API key (optional)",
      "description": "Enables gemini/ models referenced in EMBEDDING_MODELS / SUMMARY_MODELS. Free tier available. https://aistudio.google.com/apikey",
      "sensitive": true,
      "required": false
    },
    "OPENAI_API_KEY": {
      "type": "string",
      "title": "OpenAI API key (optional)",
      "description": "Enables openai/ models referenced in EMBEDDING_MODELS / SUMMARY_MODELS. https://platform.openai.com/api-keys",
      "sensitive": true,
      "required": false
    },
    "COHERE_API_KEY": {
      "type": "string",
      "title": "Cohere API key (optional)",
      "description": "Enables cohere/ embedding models referenced in EMBEDDING_MODELS. https://dashboard.cohere.com/api-keys",
      "sensitive": true,
      "required": false
    }
  },
  "author": {
    "name": "n24q02m",
    "url": "https://github.com/n24q02m"
  },
  "homepage": "https://github.com/n24q02m/crg",
  "mcpServers": {
    "better-code-review-graph": {
      "command": "uvx",
      "args": [
        "--python",
        "3.13",
        "better-code-review-graph"
      ],
      "env": {
        "MCP_TRANSPORT": "stdio",
        "EMBEDDING_MODELS": "${user_config.EMBEDDING_MODELS}",
        "LOCAL_EMBEDDING_MODEL": "${user_config.LOCAL_EMBEDDING_MODEL}",
        "LOCAL_EMBEDDING_DIM": "${user_config.LOCAL_EMBEDDING_DIM}",
        "LOCAL_RERANK_MODEL": "${user_config.LOCAL_RERANK_MODEL}",
        "LOCAL_EMBEDDING_MODEL_FILE": "${user_config.LOCAL_EMBEDDING_MODEL_FILE}",
        "LOCAL_EMBEDDING_POOLING": "${user_config.LOCAL_EMBEDDING_POOLING}",
        "LOCAL_EMBEDDING_NORMALIZE": "${user_config.LOCAL_EMBEDDING_NORMALIZE}",
        "SUMMARY_MODELS": "${user_config.SUMMARY_MODELS}",
        "EMBEDDING_API_BASE": "${user_config.EMBEDDING_API_BASE}",
        "LLM_API_BASE": "${user_config.LLM_API_BASE}",
        "OPENROUTER_API_KEY": "${user_config.OPENROUTER_API_KEY}",
        "JINA_AI_API_KEY": "${user_config.JINA_AI_API_KEY}",
        "GEMINI_API_KEY": "${user_config.GEMINI_API_KEY}",
        "OPENAI_API_KEY": "${user_config.OPENAI_API_KEY}",
        "COHERE_API_KEY": "${user_config.COHERE_API_KEY}"
      }
    }
  }
}
